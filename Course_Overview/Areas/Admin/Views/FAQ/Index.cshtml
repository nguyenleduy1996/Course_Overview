@using LModels
@model IEnumerable<FAQ>

<div class="row">
	<h4 class="card-title">FAQ List</h4>
	<div class="text-end">
		<a class="btn btn-secondary" asp-action="Create" asp-area="Admin">
			<i class="bi bi-plus-circle"></i> Create a new FAQ
		</a>
	</div>

	<table class="table table-striped" id="fAQTable">
		<thead>
			<tr>
				<th class="text-center">FAQID</th>
				<th class="text-center">Question</th>
				<th class="text-center">Answer</th>
				<th class="text-center">Actions</th>
			</tr>
		</thead>
		@* <tbody>
		@foreach (var item in Model)
		{
		<tr>
		<td>@item.FAQID</td>
		<td>@item.Question</td>
		<td>
		<div class="description-container" id="answer_@item.FAQID">
		@item.Answer
		</div>
		<button class="btn btn-link p-0" onclick="showFullAnswer('@item.FAQID')">Read more</button>
		</td>
		<td>
		<a class="btn btn-warning" asp-area="Admin" asp-route-id="@item.FAQID" asp-action="Update">
		<i class="bi bi-pencil-fill"></i>
		Update
		</a>
		<button class="btn btn-danger" onclick="deleteFAQ('@item.FAQID')">
		<i class="bi bi-trash3"></i>
		Delete
		</button>
		</td>
		</tr>
		}

		</tbody> *@
	</table>
</div>

@section scripts {
	@*Đoạn mã hiển thị thông báo alert*@
	<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
	<script src="~/AdminLayout/assets/js/Notification.js"></script>
	<script src="~/AdminLayout/assets/js/FAQ.js"></script>

	<script>
		function showFullAnswer(id) {
			var descriptionElement = document.getElementById("answer_" + id);
			Swal.fire({
				title: 'Answer',
				text: descriptionElement.textContent,
				icon: 'info',
				confirmButtonText: 'Close'
			});
		}
	</script>
}
<style>
	.description-container {
		overflow: hidden;
		text-overflow: ellipsis;
		white-space: nowrap;
		width: 200px;
		display: inline-block;
		vertical-align: middle;
	}
</style>